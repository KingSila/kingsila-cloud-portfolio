# ---------------------------------------------------------------------
# Environment Variables & Secrets
# ---------------------------------------------------------------------
.env
.env.local
*.env

# ---------------------------------------------------------------------
# Terraform Files
# ---------------------------------------------------------------------

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data

*.tfvars.json
# BUT allow auto-loading terraform files (committed, non-sensitive config)
!*.auto.tfvars
!*.auto.tfvars.json

# Ignore override files as they are usually used to override resources locally
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Lock files in modules (not in environments)
infra/modules/**/.terraform.lock.hcl

# Additional Terraform workspace and debug files
terraform.tfstate.backup
*.backup
terraform.log
terraform.tfvars.d/

# Security and documentation files (local only)
SECURITY_PRACTICES.md
README_local.md

# Local secrets, credentials, or IP configs
secrets/
local.secrets.*
*.sensitive.json

# Temporary Terraform experiment folders
scratch/
labs/
playground/

# Optional: Ignore personal notes or testing configs
*_notes.md

# Terraform plans
*.plan
*.tfplan

.fake
infra/governance/terraform.tfvars
