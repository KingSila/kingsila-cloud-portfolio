# ---------------------------------------------------------------------
# KingSila Cloud Portfolio – Environment Template
# ---------------------------------------------------------------------
# Copy this file to ".env" and fill in your actual values locally.
# NEVER commit your real .env file to GitHub (it's ignored by .gitignore).

# ========== AZURE AUTHENTICATION ==========
ARM_CLIENT_ID="<your-service-principal-client-id>"
ARM_CLIENT_SECRET="<your-service-principal-secret>"
ARM_SUBSCRIPTION_ID="<your-azure-subscription-id>"
ARM_TENANT_ID="<your-tenant-id>"

# ========== PROJECT CONTEXT ==========
AZURE_REGION="westeurope"
RESOURCE_GROUP_NAME="rg-kingsila-dev"
STORAGE_ACCOUNT_NAME="tfstatekingsila"
CONTAINER_NAME="state"

# ========== PIPELINES / GITHUB ACTIONS ==========
# These are optional; define in GitHub Secrets for production CI/CD.
GH_PAT="<your-github-personal-access-token>"
GH_ENVIRONMENT="dev"

# ========== TERRAFORM ==========
TF_LOG="INFO"            # Options: TRACE, DEBUG, INFO, WARN, ERROR
TF_VAR_environment="dev"
TF_VAR_owner="KingSila"
TF_VAR_project="CloudPortfolio"

# ========== DOCKER / AKS (optional later) ==========
ACR_NAME="acrkingsila"
AKS_CLUSTER_NAME="aks-kingsila-dev"
CONTAINER_IMAGE="kingsila/app:latest"

# ========== MONITORING ==========
LOG_ANALYTICS_WORKSPACE_NAME="law-kingsila"
APP_INSIGHTS_NAME="appi-kingsila"

# ---------------------------------------------------------------------
# Notes:
# - Keep secrets out of GitHub.
# - Use `source .env` (Linux/Mac) or `setx` (Windows) when testing locally.
# - GitHub Actions and Azure DevOps can pull secrets from GitHub Secrets or Key Vault.
# ---------------------------------------------------------------------
